# --- CORE REQUIREMENTS ---
opencv-python  # Note for Linux/VPS users: if you get a 'libGL.so.1' error, either run 'sudo apt install libgl1-mesa-glx' or replace this line with 'opencv-python-headless'
# [CRITICAL] Numpy must be exactly 1.26.1 for Watermark Logic to interact with OpenCV correctly. DO NOT UPGRADE.
numpy==1.26.1
google-generativeai
python-dotenv
python-telegram-bot[job-queue]
httpx
yt-dlp
gradio
Pillow
requests
protobuf
pyasn1
python-multipart
setuptools
tqdm
urllib3
# --- AUDIO & VIDEO PROCESSING ---
# (Note: FFmpeg is required to be installed on the OS system path)
# The engine uses raw subprocess FFmpeg calls instead of moviepy for max performance

# --- BACKGROUND TASKS & HEALTH ---
apscheduler
psutil

# --- UPLOADER & CLOUD APIS ---
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# --- TEXT TO SPEECH (TTS) ---
gTTS
edge-tts
# azure-cognitiveservices-speech  # Optional: For premium Azure TTS
# kokoro  # Optional: For Kokoro-82M TTS

# --- DATA MANIPULATION & AI MODULES (Optional/Advanced) ---
pandas
scikit-learn
#torch
#realesrgan
#basicsr
#gfpgan

# ======= STABLE FALLBACK VERSIONS =======
# opencv-python==4.8.1.78
# [CRITICAL] Numpy must be exactly 1.26.1 for Watermark Logic to interact with OpenCV correctly. DO NOT UPGRADE.
# numpy==1.26.1
# google-generativeai==0.8.5
# python-dotenv==1.0.0
# python-telegram-bot==21.0
# httpx==0.28.1
# yt-dlp==2026.2.21
# gradio==6.3.0
# apscheduler==3.10.4
# Pillow>=10.3.0
# pandas==2.2.2
# scikit-learn==1.5.0
# torch==2.10.0
